might not need beer,liquor, etc. might just leave it at none and alc.
####Make a program that converts the objects into JSON so i can switch it from FRAME to javascript at a later date
I can either make my own primitive encryption or just use a library to hash for me.
##might not need dataBase class.



here is what im using.
