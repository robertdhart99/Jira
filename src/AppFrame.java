import javax.swing.*;

public class AppFrame extends JFrame {
    //all frame applications go here- user login - main panel - add drink.
    public static final int WIDTH = 700;
    public static final int HEIGHT = 500;

    public AppFrame(){
        setSize(WIDTH, HEIGHT);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        //add all panels and labels here
        // login panel
            // maybe just instantiate the panels here then use a loginSetup() method
        //main panel
        //add drink panel
        //actions

    }
}
